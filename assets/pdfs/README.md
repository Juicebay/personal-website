# PDFs

Drop PDFs here and link to them from `publications.html` or `projects.html`
using a path like:

```html
<a href="assets/pdfs/your-file.pdf" target="_blank" rel="noopener">PDF</a>
```

Keep filenames simple with no spaces, for example `lakes-bc.pdf`.
