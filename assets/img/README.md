# Profile photo

Drop your photo in this folder as **`profile.jpg`** (a square or portrait crop
works best). The homepage will use it automatically once the file is here.

If you prefer a different filename, update the `src` of the profile image in
`index.html`.
